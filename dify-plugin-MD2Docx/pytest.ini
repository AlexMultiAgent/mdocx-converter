[pytest]
testpaths = tests
python_files = test_*.py smoke_test.py
python_functions = test_*
addopts = -v --tb=short
markers =
    slow: tests that require the pandoc binary from the pypandoc-binary wheel
    integration: tests that call external APIs (Mermaid Ink)
